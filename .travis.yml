language: node_js
node_js:
  - "0.12"
  - "0.10"
  - "0.8"
before_install:
  - npm install -g grunt-cli
  - npm install -g casperjs
install: npm install
before_script:
  - grunt build
  - phantomjs --version
  - casperjs --version
script: npm test
